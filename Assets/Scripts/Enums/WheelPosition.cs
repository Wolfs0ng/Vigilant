using UnityEngine;

namespace Vigilant.Enums
{ 
    /// <summary>
    /// Enum that contains the positions of the wheels relative to the car.
    /// </summary>
    public enum WheelPosition
    {
        FrontLeft,
        FrontRight,
        RearLeft,
        RearRight
    }
}